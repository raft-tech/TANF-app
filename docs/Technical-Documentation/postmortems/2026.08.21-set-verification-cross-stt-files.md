# Login.gov SET signing verification and cross-STT datafile submissions

- Author: Jan Timpe
- Postmortem Attendees: Kenny McNett, Alexandra Pennington, Jan Timpe, Mo Sohani, Matt Anderson, Victoria Amoroso, Miles Reiter
- Incident Date: August 13, 2026 - August 21, 2026
- Postmortem Date: August 26, 2026

## Timeline

- 08/13/2026, 9:50pm ET - First reported by a security researcher unaffiliated with the team
  - Sent an email to Alex regarding two vulnerabilities in TDP
  - Noted that they first disclosed via HHS Vulnerability Disclosure Program portal, but the report was closed as not being related to HHS systems
  - Email sent to resource mailbox, which isn't checked as often
  - Two issues reported
    - Login.gov SET key verification - unauthenticated users can send self-signed Login.gov Security Event Tokens for any user in the system, disabling the account or changing account details
    - Cross-STT data file submission - authorized users can submit data files to locations they are not authorized for
- 08/17/2026, 11:20am ET - Alex forwarded disclosure email to Jan and Kenny
- 08/17/2026, 1:40pm ET - Jan researched the issues, confirmed vulnerabilities, and spun up dev tickets
- 08/17/2026, 1:40pm ET - Jan and Alex attempted to determine if vulnerabilities were exploited in production (notes in Impact section below)
- 08/17/2026, 2:00pm ET - Jan covered the vulnerabilities in Office Hours with Mo and Matt and together came up with approach to address
- 08/18/2026, 2:30pm ET - Kenny and Jan created Github Security Advisories covering the issues
  - Login.gov SET key verification: [GHSA-w5f9-6g83-v6mc](https://github.com/raft-tech/TANF-app/security/advisories/GHSA-w5f9-6g83-v6mc)
  - Cross-STT data file submissions: [GHSA-52h9-r64r-4jq3](https://github.com/raft-tech/TANF-app/security/advisories/GHSA-52h9-r64r-4jq3)
- 08/19/2026, 11:45am ET - Jan implemented fixes for both vulnerabilities and requested reviews from Mo and Matt
- 08/21/2026, 9:50am ET - PR approvals were provided by Mo and Matt
- 08/21/2026, 12:00pm ET - Jan merged the fix into `release/v4.23.0` and created a new `release/v4.23.1`
- 08/21/2026, 12:35pm ET - Jan discarded `release/v4.23.1` and republished the fix as `release/v4.23.2`
  - Jan mistakenly based the hotfix off of `develop`, rather than `release/v4.23.0`, so the merge contained all commits to `develop` since the last release, rather than just the intended commit.
  - This could have been avoided by rebasing the hotfix branch to `release/v4.23.0`, but this was not done.
  - This was resolved by cherry-picking the hotfix commits from `release/v4.23.0` to the new `release/4.23.2` branch, which was correctly based off `release/v4.23.0` before the botched merge.
- 08/21/2026, 1:00pm ET - Alex merged and tested `release/v4.23.2`.
- 08/21/2026, 1:45pm ET - Alex noticed an implementation error and requested a change from Jan
  - `verify_signature` was set to `settings.DEBUG`, meaning the JWT would only be signature-verified if `DEBUG=True`. `DEBUG=False` in staging and production environments, so this was the opposite of the intended effect.
  - This was missed in testing because Jan only tested with a hardcoded `False` before adding in the `settings.DEBUG` call. This was also missed in PR review.
- 08/21/2026, 1:55pm ET - Jan created a hotfix `release/v4.23.3` with the change
- 08/21/2026, 2:20pm ET - Alex merged and tested `release/v4.23.3`
- 08/21/2026, 2:40pm ET - Alex noticed some test failures with the go parser while merging (unrelated to patch fixes)
  - Jan re-ran tests locally and in Raft's CircleCI, and all tests passed
  - The failures were related to Go parser integration tests, which would not have been affected by the changes made in the hotfix. They were determined to be unrelated and Alex proceeded with the merge despite the failing tests.
- 08/21/2026, 4:20pm ET - Alex approved the release to production
- 08/21/2026, 4:40pm ET - Jan attempted to validate the release in staging, but noticed the Login.gov configuration had an error
  - The "Push Notification URL" setting in the Login.gov Partner Portal was pointing to the old domain names (`tdp-backend-staging.acf.hhs.gov` rather than `staging.tanfdata.acf.hhs.gov`)
  - Jan made the Login.gov configuration changes in the Partner Portal
  - Validation still failed, the backend logs showed no indication that a call was made to the event-token endpoint. This was never resolved and assumed to be a propagation delay from the config change
- 08/21/2026, 4:50pm ET - Jan attempted to validate the release in production, but noticed the app's backend environment variables were wrong and created an error
  - The default setting for `LOGIN_GOV_SET_AUDIENCE` (the Push Notification URL used in the Login.gov Partner Portal configuration) had a trailing `/`. This redirects to the correct endpoint, but the POST request body cannot be preserved, so the request failed.
  - The setting is able to be overridden by providing the correct `LOGIN_GOV_SET_AUDIENCE` in the backend environment variables
- 08/21/2026, 5:00pm ET - Jan requested Alex update the production app's environment variables and restart
- 08/21/2026, 5:10pm ET - Jan successfully validated the release in production
  - This was done by requesting a password reset for the Login.gov account. Once the password has been reset, a Security Event Token is created in TDP to log the event
- 08/21/2026, 5:15pm ET - Jan published the draft Security Advisories and credited the finder
- 08/21/2026, 5:15pm ET - Incident resolved

## Root cause

### Login.gov SET key verification

Full details can be found in the Security Advisory: [GHSA-w5f9-6g83-v6mc](https://github.com/raft-tech/TANF-app/security/advisories/GHSA-w5f9-6g83-v6mc)

Original implementation of Login.gov Security Event Tokens had "verify_signature": False in the JSON Web Token (JWT) decode. This means a self-signed JWT could be used to create SETs, instead of only a Login.gov-signed JWT.

PR [#5303](https://github.com/raft-tech/TANF-app/pull/5303), [Line](https://github.com/raft-tech/TANF-app/blob/119285520a28f33ab923ce340ab2df05c7a79213/tdrs-backend/tdpservice/security/views.py#L112) - Merged 10-25-2025, released in [v4.5.0](https://github.com/raft-tech/TANF-app/releases/tag/v4.5.0)

This was likely an artifact of testing - we cannot properly sign Json Web Tokens with the Login.gov private key when testing locally or in test environments, so `verify_signature` must be `False`. This flag was likely committed and merged by mistake and was not caught in PR review.

- Is there a way to verify things like this in the pipeline? on commit hooks, secrets check, etc. - Husky?

### Cross-STT data file submissions:

Full details can be found in the Security Advisory: [GHSA-52h9-r64r-4jq3](https://github.com/raft-tech/TANF-app/security/advisories/GHSA-52h9-r64r-4jq3)

Original implementation of get_requested_stt permissions used `ChainMap(view.kwargs, request.query_params, request.data)`, meaning `query_params` was validated before the `POST` body for data file submissions. This could be used to change which STT the file was submitted for, bypassing the permission guard by using the STT allowed for the user in the `query_params`.

PR [#1286](https://github.com/raft-tech/TANF-app/pull/1286), [Line](https://github.com/raft-tech/TANF-app/blob/420efa35411cf94c798d28e4796c516fec031584/tdrs-backend/tdpservice/users/permissions.py#L96) - Merged 10-19-2021, released in [v1.0.0](https://github.com/raft-tech/TANF-app/releases/tag/v1.0.0)

This could have technically been exploited in any version of TDP by modifying requested in DevTools to include the `?stt=X` query parameter and changing the `POST` body STT to the target location. However, it was made more easily exploitable when API authentication was introduced via PR [#5814](https://github.com/raft-tech/TANF-app/pull/5814), merged 05-27-2026, released in [v4.20.0](https://github.com/raft-tech/TANF-app/releases/tag/v4.20.0)

This was seemingly an implementation mistake. `ChainMap` maps all the given parameters, regardless of request type, and gives preference to the `query_params` (since they come first). This allows the `query_params` value to override what is provided in the `POST` body for the authorization step, but the `POST` body is used to create the object in the serialization step.

## Impact

We have not yet found any indication of either of these issues being exploited, but system logs are not comprehensive for either.

### Login.gov SET key verification

When decoding a JSON Web Token (JWT), `verify_signature` allows the system to determine whether the JWT was signed by a specific private key by using a public key. In our case, Login.gov signs the JWT with their private key and we use their public key to validate the JWT actually came from Login.gov and was not modified.

Without `verify_signature`, we do not determine if the key was signed by Login.gov. An attacker could submit any self-signed JWT with any payload they want and our system would have treated it as legitimate. The `/v1/security/event-token` endpoint requires no authentication by design, since Login.gov cannot authenticate to our system before submitting a request. The JWT signing is meant as a substitute for authorization. This means any attacker could create a Security Event Token for any account in the system, provided they know the target account's Login.gov UUID (information required in the payload).

The most impactful result possible was that an attacker could disable the target user's account. Other possible actions are enabling the target account and changing the target account's email address. Other SET types only log a message and do not change account details.

We checked the created Security Event Tokens in the Django Admin Console for any suspicious operations. We see the following counts for each event

- `ACCOUNT_DISABLED`: 0 events
- `ACCOUNT_PURGED`: 4 events
- `EMAIL_CHANGED`: 5 events
  - Some of these events had no associated user, as late as 8/25/2026 - investigate https://github.com/raft-tech/TANF-app/pull/5862
- `EMAIL_RECYCLED`: 3 events

Because the logs cannot tell us who created each event token, there's no way for us to tell if the events are created legitimately or maliciously. The best option is to monitor user reports for locked-out accounts and investigate/respond individually. No such reports were mentioned.

What would help catch this in the future?

- Regular security audits, security audit tools
- Security pentesting - supposed to be done every year, hasn't been done in a couple years
- Verify signature setting in the pipeline via git hooks, secrets check, Husky, etc.

### Cross-STT data file submissions

Because the STT provided in `query_params` in a POST request to the `/v1/data-files` endpoint were taking precidence over the STT provided in the request body, a user could submit a file to a location for which they are not authorized.

This required the user to be authorized for the location used in `query_params`, even if they were not authorized for the location provided in the `POST` body, limiting the potential impact.
We could audit all submitted data files to find where a file's `submitted_by` user belonged to a location different than the file was submitted for. Alex did this, only 1 user submitted for a jurisdiction that was not their own (Arizona, which is authorized to submit for Tribes). The `submitted_by` user could technically be swapped as well, but would require knowledge of another user's UUID (unlikely).

Another option is to monitor user reports for files submitted for an STT that did not belong to that STT. We did not receive any such reports.

What would help catch this in the future?

- If data for a particular STT looks incorrect (unless submitted something plausible) - feedback reports done regularly
- Exceptions table - STTs that a user is allowed to submit for
- Developers/Code review - validate the information that is used to create the object, don't validate query params over post data, etc

## Resolution steps

Both fixes were addressed in a single hotfix PR, with one added patch to invert the `settings.DEBUG` value as noted in the timeline. The hotfix was released 08-21-2026 at 4:20pm ET and validated in production at 5:15pm ET.

### Login.gov SET key verification

Enabled `verify_signature` in staging and production environments by using `not settings.DEBUG`. This enforces JWT key verification when the setting is `False`, but still enables local testing.

### Cross-STT data file submissions

Updated the `get_requested_stt` function to check `request.method` and apply `query_parameters` to the `ChainMap` only on `GET` requests, and use only the `POST` body on `POST` requests. This allows the function to still be used for both request types, but limits authorization to only the fields used by the subsequent queries/serializers.

## Moving forward

- Github security disclosures worked well as they allowed us to create private issues and PRs to track work without publishing until we were ready to do so
  - created on Raft side, not HHS. made public as soon as we merged to raft.
- Do we request a CVE ID?
  - Might help security researchers understand which vulns in which applications
  - Might help security researchers find more vulns in our app because we give them credit
  - Not publishing library/package used by downstream systems
- Update `SECURITY.md` disclosure steps (not submit to HHS unless it is an HHS platform issue)
  - Alex to reach out to HHS Disclosure Program to clarify why it was out of scope
    hosted on HHS system? closed-source HHS codebase?
  - Which email address vulns are reported to, or have access to proper mailbox at all times. Reported to
    - `info@teamraft.com` -> create dedicated project email address
    - `TANFData@acf.hhs.gov`
- Update hotfix process docs to be more specific about
  - branching/rebasing
  - merge and backport merge
  - release semvar patch increment
- Add more logging/notification so that we can tell if issues are exploited
  - May not be feasible
  - Log where query param args are different than post body, possibly create LogEntry or alert in this situation - Alex interested in being alerted to unexpected situations in general
- Testing the code as written is very important. Use the settings rather than hardcoding, and re-verify when a change is made. PR reveiwers should also manually test changes.
- Flaky tests (go parser integration)
- Look into Husky for security checks
- Look into AI/LLM security scans (Claude or Codex security scans)
- Look into Email Changed SET with no associated user - investigate https://github.com/raft-tech/TANF-app/pull/5862

## Action items

- Jan - Update SECURITY.md disclosure steps (not submit to HHS unless it is an HHS platform issue)
- Alex - reach out to HHS Disclosure Program to clarify why it was out of scope
  - hosted on HHS system?
  - closed-source HHS codebase?
  - Which email address vulns are reported to, or have access to proper mailbox at all times. Reported to
    - Kenny - `info@teamraft.com` -> create dedicated project email address. also check if we can get emails sent to MM dev channel
    - `TANFData@acf.hhs.gov` -> Alex to figure out how Resource Mailboxes are loaded on work device. Forward to HHS email address, or add to raft team email?
- Jan - Update hotfix docs
- Alex - inform security team that incident occurred, ask about security advisories (hhs vs raft repo), resuming pentesting
- All developers - review and testing notes
- Draft tickets:
  - Matt - Husky
  - Mo - AI Security Scans
  - Jan - Unassociated SETs - [#6047](https://github.com/raft-tech/TANF-app/issues/6047)
  - Jan - Flaky go-parser tests - [#6049](https://github.com/raft-tech/TANF-app/issues/6049)
  - Heiko - Log/alert unexpected conditions
