---
version: 1
applications:
- name: tdp-frontend
  memory: 256M
  instances: 1
  disk_quota: 256M
  timeout: 180
  env:
    NODE_OPTIONS: --openssl-legacy-provider
  docker:
    image: ((docker-frontend))

